Write a complete Python FastAPI backend that implements a searchable product database using SQLite.

Requirements:
Use FastAPI
Use SQLite as the database
Put everything in one runnable Python file
Include a database setup function that creates a products table
Add sample product data automatically when the app starts
Implement a GET /search endpoint that accepts a query parameter such as q
The endpoint should search product names and descriptions and return matching rows as JSON
Keep the implementation simple and minimal, suitable for a class project demo
Do not use an ORM
Use direct SQL queries with SQLite
Include all necessary imports
Add brief comments explaining each major part
Output only the code.
